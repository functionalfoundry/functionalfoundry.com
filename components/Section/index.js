import styled from 'styled-components'

import { Colors } from '../../utils/styles'

export const Header = styled.h2`color: ${Colors.first};`

export const Content = styled.div``

export default styled.section`

`
